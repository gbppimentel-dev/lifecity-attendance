Extract into your project root and replace matching files. Includes the earlier guest layout fixes. Builds on the Services and Mobile Workspaces updates. No database changes. Run npm run build, then test phone sign-in, guest navigation, service pagination, and expandable welcome help.
